{
 "scripts": {
  "vercel-build": "npm install -g pnpm && pnpm install --prefix client && pnpm --prefix client run build && pnpm install --prefix server && pnpm --prefix server exec prisma generate"
}
}