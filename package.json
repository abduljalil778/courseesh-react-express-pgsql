{
  "name": "courseesh-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:server": "cd server && npm install && npx prisma generate",
    "build:client": "cd client && npm install && npm run build",
    "vercel-build": "npm run build:server && npm run build:client"
  },
  "author": "",
  "license": "ISC"
}